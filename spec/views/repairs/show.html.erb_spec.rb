require 'rails_helper'

RSpec.describe "repairs/show", type: :view do
  before(:each) do
    # assign(:repair, Repair.create!(
    #   car: nil,
    #   mechanical_workshop: nil,
    #   repairs: "Repairs"
    # ))
  end

  it "renders attributes in <p>" do
    # render
    # expect(rendered).to match(//)
    # expect(rendered).to match(//)
    # expect(rendered).to match(/Repairs/)
  end
end
